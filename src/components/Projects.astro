---
const projects = [
  {
    name: "Multi-Region Cloud Platform",
    summary:
      "Built an active-active platform with automated failover, cutting recovery time to minutes.",
    tags: ["AWS", "Terraform", "ArgoCD"],
    href: "https://github.com/",
  },
  {
    name: "FinOps Cost Optimizer",
    summary:
      "Automated cost anomaly detection and rightsizing recommendations for 40+ services.",
    tags: ["Python", "BigQuery", "Cloud Run"],
    href: "https://github.com/",
  },
  {
    name: "Edge Observability Suite",
    summary:
      "Unified metrics, logs, and traces into a single dashboard with alert routing.",
    tags: ["Grafana", "Prometheus", "Loki"],
    href: "https://github.com/",
  },
];
---

<section class="mx-auto w-full max-w-6xl px-6 py-16" id="projects">
  <div class="flex flex-wrap items-end justify-between gap-6">
    <div class="space-y-3">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">
        Projects
      </p>
      <h2 class="font-display text-3xl font-semibold text-slate-900">
        Recent builds focused on scale and reliability.
      </h2>
    </div>
    <a
      href="/projects"
      class="inline-flex items-center gap-2 text-sm font-semibold text-slate-700 transition hover:text-slate-900"
    >
      See all projects
      <iconify-icon icon="ph:arrow-right-bold"></iconify-icon>
    </a>
  </div>

  <div class="mt-10 grid gap-6 md:grid-cols-3">
    {projects.map((project) => (
      <article class="flex h-full flex-col rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="flex items-center justify-between">
          <h3 class="font-display text-xl font-semibold text-slate-900">
            {project.name}
          </h3>
          <iconify-icon icon="ph:rocket-launch" class="text-xl text-slate-400"></iconify-icon>
        </div>
        <p class="mt-4 text-sm text-slate-600">{project.summary}</p>
        <div class="mt-6 flex flex-wrap gap-2">
          {project.tags.map((tag) => (
            <span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
              {tag}
            </span>
          ))}
        </div>
        <a
          href={project.href}
          class="mt-auto inline-flex items-center gap-2 pt-6 text-sm font-semibold text-slate-700 transition hover:text-slate-900"
        >
          View on GitHub
          <iconify-icon icon="ph:arrow-up-right-bold"></iconify-icon>
        </a>
      </article>
    ))}
  </div>
</section>
