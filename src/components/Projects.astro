---
const projects = [
  {
    name: "Multi-Region Cloud Platform",
    summary:
      "Designed a multi-region foundation with automated failover and routine resilience testing.",
    tags: ["AWS", "Terraform", "ArgoCD"],
    href: "https://github.com/",
  },
  {
    name: "Cost Governance Program",
    summary:
      "Implemented cost visibility and right-sizing workflows across core services.",
    tags: ["FinOps", "BigQuery", "Cloud Run"],
    href: "https://github.com/",
  },
  {
    name: "Observability Platform",
    summary:
      "Unified metrics, logs, and traces with clear alerting and escalation paths.",
    tags: ["Grafana", "Prometheus", "Loki"],
    href: "https://github.com/",
  },
];
---

<section class="mx-auto w-full max-w-6xl px-6 py-16" id="projects">
  <div class="flex flex-wrap items-end justify-between gap-6">
    <div class="space-y-3">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">
        Projects
      </p>
      <h2 class="font-display text-3xl font-semibold text-white">
        Recent work focused on reliability and clarity.
      </h2>
    </div>
    <a
      href="/projects"
      class="inline-flex items-center gap-2 text-sm font-semibold text-slate-300 transition hover:text-white"
    >
      See all projects
      <iconify-icon icon="ph:arrow-right-bold"></iconify-icon>
    </a>
  </div>

  <div class="mt-10 grid gap-6 md:grid-cols-3">
    {projects.map((project) => (
      <article class="flex h-full flex-col rounded-3xl border border-slate-800 bg-slate-900/60 p-6 shadow-sm">
        <div class="flex items-center justify-between">
          <h3 class="font-display text-xl font-semibold text-white">
            {project.name}
          </h3>
          <iconify-icon icon="ph:rocket-launch" class="text-xl text-slate-500"></iconify-icon>
        </div>
        <p class="mt-4 text-sm text-slate-300">{project.summary}</p>
        <div class="mt-6 flex flex-wrap gap-2">
          {project.tags.map((tag) => (
            <span class="rounded-full border border-slate-800 bg-slate-900 px-3 py-1 text-xs font-semibold text-slate-300">
              {tag}
            </span>
          ))}
        </div>
        <a
          href={project.href}
          class="mt-auto inline-flex items-center gap-2 pt-6 text-sm font-semibold text-slate-300 transition hover:text-white"
        >
          View details
          <iconify-icon icon="ph:arrow-up-right-bold"></iconify-icon>
        </a>
      </article>
    ))}
  </div>
</section>
