---
import {CV_DATA} from '../data/cv';

const { data = CV_DATA } = Astro.props;
const {summary} = data;
---
<section class="flex min-h-0 flex-col gap-y-3 print:gap-y-1">
    <h2 class="text-xl font-bold" id="about-section">About</h2>
    <div class="text-pretty text-sm text-foreground/80 print:text-[12px]"
         aria-labelledby="about-section">
        {summary || <p class="italic">Professional summary and core background information will be provided here.</p>}
    </div>
</section>
