---
const socials = [
  { label: "GitHub", href: "https://github.com/zahidabdillahdev", icon: "ph:github-logo" },
  { label: "Email", href: "mailto:contact@zahidabdillah.dev", icon: "ph:envelope-simple" },
];
---

<footer class="border-t border-slate-800 bg-slate-950" id="contact">
  <div class="mx-auto w-full max-w-6xl px-6 py-12">
    <div class="grid gap-8 md:grid-cols-2">
      <div class="space-y-4 pr-4">
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">
          Contact
        </p>
        <h2 class="font-display text-2xl font-semibold text-white">
          Let’s discuss your next infrastructure milestone.
        </h2>
        <p class="text-slate-300">
          Available for consulting, reviews, and ongoing platform support.
        </p>
      </div>
      <div class="space-y-6">
        <form
          class="grid gap-4 rounded-2xl border border-slate-800 bg-slate-900/60 p-5"
          action="mailto:contact@zahidabdillah.dev"
          method="post"
          enctype="text/plain"
        >
          <div class="grid gap-2">
            <label class="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500" for="contact-name">
              Name
              <span class="ml-1 text-rose-400">*</span>
            </label>
            <input
              class="rounded-xl border border-slate-700 bg-slate-950/80 px-4 py-2 text-sm text-slate-100 focus:border-slate-500 focus:outline-none"
              id="contact-name"
              name="name"
              required
              type="text"
            />
          </div>
          <div class="grid gap-2">
            <label class="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500" for="contact-email">
              Email
              <span class="ml-1 text-rose-400">*</span>
            </label>
            <input
              class="rounded-xl border border-slate-700 bg-slate-950/80 px-4 py-2 text-sm text-slate-100 focus:border-slate-500 focus:outline-none"
              id="contact-email"
              name="email"
              required
              type="email"
            />
          </div>
          <div class="grid gap-2">
            <label class="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500" for="contact-message">
              Message
            </label>
            <textarea
              class="min-h-[120px] rounded-xl border border-slate-700 bg-slate-950/80 px-4 py-2 text-sm text-slate-100 focus:border-slate-500 focus:outline-none"
              id="contact-message"
              name="message"
              rows="4"
            ></textarea>
          </div>
          <button
            class="inline-flex items-center justify-center gap-2 rounded-full bg-slate-100 px-5 py-2 text-sm font-semibold text-slate-900"
            type="submit"
          >
            Send message
            <iconify-icon icon="ph:paper-plane-tilt-bold"></iconify-icon>
          </button>
        </form>
        <div class="flex items-center justify-center gap-4 text-slate-300">
          {socials.map((item) => (
            <a
              class="flex h-11 w-11 items-center justify-center rounded-full border border-slate-800 bg-slate-900/60 transition hover:border-slate-500 hover:text-white"
              href={item.href}
              aria-label={item.label}
            >
              <iconify-icon icon={item.icon} class="text-xl"></iconify-icon>
              <span class="sr-only">{item.label}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
    <div class="mt-10 border-t border-slate-800 pt-6 text-center text-xs text-slate-500">
      © 2026 Zahid Abdillah. All rights reserved.
    </div>
  </div>
</footer>
